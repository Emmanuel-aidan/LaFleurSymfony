<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
      <a class="navbar-brand" href="{{path('acceuil_news')}}">LA FLEUR              </a>
      {% if app.user %}
        <div class='navbar-brand' style="font-size:12px" href='#'>    connecté en tant que {{app.user.prenom}}
        {% if is_granted('ROLE_ADMIN') %}
          (ADMIN)
        {% endif %}  
        </div>  
      {% endif %}
       
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
    
      {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <div class="btn-group">
            <button type="button" class="btn btn-sm btn-warning dropdown-toggle" data-toggle="dropdown">
                Gestion <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li><a href="">News</a></li>
              <li><a href="{{path('utilisateur_index')}}">Utilisateurs</a></li>
            </ul>
          </div>
        </li>
    {% endif %}  
    
          <li class="nav-item active">
            <a class="nav-link" href="{{path('acceuil_news')}}">Accueil
              <span class="sr-only">(current)</span>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{path('index_catalogue')}}">Catalogue</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="">Contact</a>
          </li>
           

           {% if app.user %}
             <li class="nav-item">
            <a class="nav-link" href="">Profil</a>
          </li>
           {% endif %}
          <li class="nav-item">
          {% if app.user %}
            <a class="nav-link" href="{{path('app_logout')}}">Déconnexion</a>
          {% else %} 
            <a class="nav-link" href="{{path('app_login')}}">Connexion</a>
            {% endif %}
          </li>

          <li class="nav-item">
                 <a href="{{path('index_panier')}}" type='button' class='btn btn-primary'>
                             <i class='fas fa-shopping-cart'>
                             {{ render(controller('App\\Controller\\PanierController::nbProdPanier')) }}
                             </i></a>
          </li>
        </ul>
      </div>
    </div>
  </nav>